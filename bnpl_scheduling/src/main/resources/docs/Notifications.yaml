
securitySchemes:
    bearerAuth:  # arbitrary name for the security scheme
      type: http
      scheme: bearer
      bearerFormat: JWT 
paths:
  /notifications:
    post:
      security:
      - bearerAuth: []
      tags:
        - notifications
      summary: Add new Notifications
      produces:
        - application/json
      parameters:
        - in: body
          name: displayName
          description: Save new Notifications
          required: true
          schema:
            $ref: '#/definitions/notifications'
      responses:
        '201':
          description: An object with Notifications details

  /notifications/:
    get:
      security:
          - bearerAuth: []
      tags:
        - notifications
      summary: List Notifications
      description: Search Notifications 
      parameters:
        - name: page
          in: query
          description: page No
          required: false
          explode: true
          schema:
            type: integer
        - name: limit
          in: query
          description: limit data per page or Page Size
          required: false
          explode: true
          schema:
            type: integer
        
        - name: notificationContent
          in: query
          description: notificationContent values that need to be considered for filter
          required: false
          explode: true
          schema:
            type: string
        - name: notificationContent_mode
          in: query
          description: notificationContent mode values that need to be considered for filter type greater than,less than,equal
          required: false
          explode: true
          schema:
            type: string
            default: contains
            enum:
              - equals
              - contains
              - startsWith
              - endsWith
              - notContains
        - name: createBy
          in: query
          description: createBy values that need to be considered for filter
          required: false
          explode: true
          schema:
            type: string
        - name: createBy_mode
          in: query
          description: createBy mode values that need to be considered for filter type greater than,less than,equal
          required: false
          explode: true
          schema:
            type: string
            default: contains
            enum:
              - equals
              - contains
              - startsWith
              - endsWith
              - notContains
        - name: updateBy
          in: query
          description: updateBy values that need to be considered for filter
          required: false
          explode: true
          schema:
            type: string
        - name: updateBy_mode
          in: query
          description: updateBy mode values that need to be considered for filter type greater than,less than,equal
          required: false
          explode: true
          schema:
            type: string
            default: contains
            enum:
              - equals
              - contains
              - startsWith
              - endsWith
              - notContains
        - name: status
          in: query
          description: status values that need to be considered for filter
          required: false
          explode: true
          schema:
            type: integer
        - name: status_mode
          in: query
          description: status mode values that need to be considered for filter type greater than,less than,equal
          required: false
          explode: true
          schema:
            type: string
            default: equals
            enum:
              - equals
              - notEquals
              - lt
              - lte
              - gt
              - gte
      responses:
        '200':
          description: successful operation
          content:
            application/json:
              schema:
                type: object
                properties:
                  count:
                    type: integer
                  perpage:
                    type: integer
                  page:
                    type: integer
                  docs:
                    type: array
                    items:
                      $ref: '#/definitions/notificationsData'
        '400':
          description: Invalid status value
/notifications/all:
    get:
      security:
          - bearerAuth: []
      tags:
        - notifications
      summary: List Notifications
      description: get all data without paging
      parameters: 
       
        - name: notificationContent
          in: query
          description: notificationContent values that need to be considered for filter
          required: false
          explode: true
          schema:
            type: string
        - name: notificationContent_mode
          in: query
          description: notificationContent mode values that need to be considered for filter type greater than,less than,equal
          required: false
          explode: true
          schema:
            type: string
            default: contains
            enum:
              - equals
              - contains
              - startsWith
              - endsWith
              - notContains
        - name: createBy
          in: query
          description: createBy values that need to be considered for filter
          required: false
          explode: true
          schema:
            type: string
        - name: createBy_mode
          in: query
          description: createBy mode values that need to be considered for filter type greater than,less than,equal
          required: false
          explode: true
          schema:
            type: string
            default: contains
            enum:
              - equals
              - contains
              - startsWith
              - endsWith
              - notContains
        - name: updateBy
          in: query
          description: updateBy values that need to be considered for filter
          required: false
          explode: true
          schema:
            type: string
        - name: updateBy_mode
          in: query
          description: updateBy mode values that need to be considered for filter type greater than,less than,equal
          required: false
          explode: true
          schema:
            type: string
            default: contains
            enum:
              - equals
              - contains
              - startsWith
              - endsWith
              - notContains
        - name: status
          in: query
          description: status values that need to be considered for filter
          required: false
          explode: true
          schema:
            type: integer
        - name: status_mode
          in: query
          description: status mode values that need to be considered for filter type greater than,less than,equal
          required: false
          explode: true
          schema:
            type: string
            default: equals
            enum:
              - equals
              - notEquals
              - lt
              - lte
              - gt
              - gte
      responses:
        '200':
          description: successful operation
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/definitions/notificationsData'
        '400':
          description: Invalid status value
/notifications/suggestions:
    get:
      security:
          - bearerAuth: []
      tags:
        - notifications
      summary: List notifications
      description: Keyward search data on notificationContent
      parameters:
        - name: search  
          in: query
          description: keyword search on notificationContent
          required: false
          explode: true
          schema:
            type: string
      responses:
        '200':
          description: successful operation
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/definitions/notificationsData'
        '400':
          description: Invalid status value
/notifications/{id}:
    get:
      security:
          - bearerAuth: []
      tags:
        - notifications
      summary: List notifications
      description: get 
      parameters:
        - name: id
          in: path
          description: get Notifications by id
          required: true
          schema:
            type: string
      responses:
        '200':
          description: successful operation
          content:
            application/json:
              schema:
                type: object
                $ref: '#/definitions/notificationsData'
        '400':
          description: Invalid status value
definitions:
  notifications:
      type: object
      properties:
        
        notificationContent:
            type: string
        readDate:
            type: string
            format: date-time
        createBy:
            type: string
        createAt:
            type: string
            format: date-time
        updateBy:
            type: string
        updateAt:
            type: string
            format: date-time
        status:
            type: number
            format: double
              

  notificationsData:
      type: object
      properties:
      
        notificationContent:
            type: string
        readDate:
            type: string
            format: date-time
        createBy:
            type: string
        createAt:
            type: string
            format: date-time
        updateBy:
            type: string
        updateAt:
            type: string
            format: date-time
        status:
            type: number
            format: double
              
    