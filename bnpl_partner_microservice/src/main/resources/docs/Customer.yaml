
securitySchemes:
    bearerAuth:  # arbitrary name for the security scheme
      type: http
      scheme: bearer
      bearerFormat: JWT 
paths:
  /customer:
    post:
      security:
      - bearerAuth: []
      tags:
        - customer
      summary: Add new Customer
      produces:
        - application/json
      parameters:
        - in: body
          name: displayName
          description: Save new Customer
          required: true
          schema:
            $ref: '#/definitions/customer'
      responses:
        '201':
          description: An object with Customer details

  /customer/:
    get:
      security:
          - bearerAuth: []
      tags:
        - customer
      summary: List Customer
      description: Search Customer 
      parameters:
        - name: page
          in: query
          description: page No
          required: false
          explode: true
          schema:
            type: integer
        - name: limit
          in: query
          description: limit data per page or Page Size
          required: false
          explode: true
          schema:
            type: integer
        
        - name: name
          in: query
          description: name values that need to be considered for filter
          required: false
          explode: true
          schema:
            type: string
        - name: name_mode
          in: query
          description: name mode values that need to be considered for filter type greater than,less than,equal
          required: false
          explode: true
          schema:
            type: string
            default: contains
            enum:
              - equals
              - contains
              - startsWith
              - endsWith
              - notContains
        - name: address
          in: query
          description: address values that need to be considered for filter
          required: false
          explode: true
          schema:
            type: string
        - name: address_mode
          in: query
          description: address mode values that need to be considered for filter type greater than,less than,equal
          required: false
          explode: true
          schema:
            type: string
            default: contains
            enum:
              - equals
              - contains
              - startsWith
              - endsWith
              - notContains
        - name: email
          in: query
          description: email values that need to be considered for filter
          required: false
          explode: true
          schema:
            type: string
        - name: email_mode
          in: query
          description: email mode values that need to be considered for filter type greater than,less than,equal
          required: false
          explode: true
          schema:
            type: string
            default: contains
            enum:
              - equals
              - contains
              - startsWith
              - endsWith
              - notContains
        - name: phone
          in: query
          description: phone values that need to be considered for filter
          required: false
          explode: true
          schema:
            type: string
        - name: phone_mode
          in: query
          description: phone mode values that need to be considered for filter type greater than,less than,equal
          required: false
          explode: true
          schema:
            type: string
            default: contains
            enum:
              - equals
              - contains
              - startsWith
              - endsWith
              - notContains
        - name: createBy
          in: query
          description: createBy values that need to be considered for filter
          required: false
          explode: true
          schema:
            type: string
        - name: createBy_mode
          in: query
          description: createBy mode values that need to be considered for filter type greater than,less than,equal
          required: false
          explode: true
          schema:
            type: string
            default: contains
            enum:
              - equals
              - contains
              - startsWith
              - endsWith
              - notContains
        - name: updateBy
          in: query
          description: updateBy values that need to be considered for filter
          required: false
          explode: true
          schema:
            type: string
        - name: updateBy_mode
          in: query
          description: updateBy mode values that need to be considered for filter type greater than,less than,equal
          required: false
          explode: true
          schema:
            type: string
            default: contains
            enum:
              - equals
              - contains
              - startsWith
              - endsWith
              - notContains
        - name: city
          in: query
          description: city values that need to be considered for filter
          required: false
          explode: true
          schema:
            type: string
        - name: city_mode
          in: query
          description: city mode values that need to be considered for filter type greater than,less than,equal
          required: false
          explode: true
          schema:
            type: string
            default: contains
            enum:
              - equals
              - contains
              - startsWith
              - endsWith
              - notContains
        - name: state
          in: query
          description: state values that need to be considered for filter
          required: false
          explode: true
          schema:
            type: string
        - name: state_mode
          in: query
          description: state mode values that need to be considered for filter type greater than,less than,equal
          required: false
          explode: true
          schema:
            type: string
            default: contains
            enum:
              - equals
              - contains
              - startsWith
              - endsWith
              - notContains
        - name: postcode
          in: query
          description: postcode values that need to be considered for filter
          required: false
          explode: true
          schema:
            type: string
        - name: postcode_mode
          in: query
          description: postcode mode values that need to be considered for filter type greater than,less than,equal
          required: false
          explode: true
          schema:
            type: string
            default: contains
            enum:
              - equals
              - contains
              - startsWith
              - endsWith
              - notContains
        - name: status
          in: query
          description: status values that need to be considered for filter
          required: false
          explode: true
          schema:
            type: integer
        - name: status_mode
          in: query
          description: status mode values that need to be considered for filter type greater than,less than,equal
          required: false
          explode: true
          schema:
            type: string
            default: equals
            enum:
              - equals
              - notEquals
              - lt
              - lte
              - gt
              - gte
      responses:
        '200':
          description: successful operation
          content:
            application/json:
              schema:
                type: object
                properties:
                  count:
                    type: integer
                  perpage:
                    type: integer
                  page:
                    type: integer
                  docs:
                    type: array
                    items:
                      $ref: '#/definitions/customerData'
        '400':
          description: Invalid status value
/customer/all:
    get:
      security:
          - bearerAuth: []
      tags:
        - customer
      summary: List Customer
      description: get all data without paging
      parameters: 
       
        - name: name
          in: query
          description: name values that need to be considered for filter
          required: false
          explode: true
          schema:
            type: string
        - name: name_mode
          in: query
          description: name mode values that need to be considered for filter type greater than,less than,equal
          required: false
          explode: true
          schema:
            type: string
            default: contains
            enum:
              - equals
              - contains
              - startsWith
              - endsWith
              - notContains
        - name: address
          in: query
          description: address values that need to be considered for filter
          required: false
          explode: true
          schema:
            type: string
        - name: address_mode
          in: query
          description: address mode values that need to be considered for filter type greater than,less than,equal
          required: false
          explode: true
          schema:
            type: string
            default: contains
            enum:
              - equals
              - contains
              - startsWith
              - endsWith
              - notContains
        - name: email
          in: query
          description: email values that need to be considered for filter
          required: false
          explode: true
          schema:
            type: string
        - name: email_mode
          in: query
          description: email mode values that need to be considered for filter type greater than,less than,equal
          required: false
          explode: true
          schema:
            type: string
            default: contains
            enum:
              - equals
              - contains
              - startsWith
              - endsWith
              - notContains
        - name: phone
          in: query
          description: phone values that need to be considered for filter
          required: false
          explode: true
          schema:
            type: string
        - name: phone_mode
          in: query
          description: phone mode values that need to be considered for filter type greater than,less than,equal
          required: false
          explode: true
          schema:
            type: string
            default: contains
            enum:
              - equals
              - contains
              - startsWith
              - endsWith
              - notContains
        - name: createBy
          in: query
          description: createBy values that need to be considered for filter
          required: false
          explode: true
          schema:
            type: string
        - name: createBy_mode
          in: query
          description: createBy mode values that need to be considered for filter type greater than,less than,equal
          required: false
          explode: true
          schema:
            type: string
            default: contains
            enum:
              - equals
              - contains
              - startsWith
              - endsWith
              - notContains
        - name: updateBy
          in: query
          description: updateBy values that need to be considered for filter
          required: false
          explode: true
          schema:
            type: string
        - name: updateBy_mode
          in: query
          description: updateBy mode values that need to be considered for filter type greater than,less than,equal
          required: false
          explode: true
          schema:
            type: string
            default: contains
            enum:
              - equals
              - contains
              - startsWith
              - endsWith
              - notContains
        - name: city
          in: query
          description: city values that need to be considered for filter
          required: false
          explode: true
          schema:
            type: string
        - name: city_mode
          in: query
          description: city mode values that need to be considered for filter type greater than,less than,equal
          required: false
          explode: true
          schema:
            type: string
            default: contains
            enum:
              - equals
              - contains
              - startsWith
              - endsWith
              - notContains
        - name: state
          in: query
          description: state values that need to be considered for filter
          required: false
          explode: true
          schema:
            type: string
        - name: state_mode
          in: query
          description: state mode values that need to be considered for filter type greater than,less than,equal
          required: false
          explode: true
          schema:
            type: string
            default: contains
            enum:
              - equals
              - contains
              - startsWith
              - endsWith
              - notContains
        - name: postcode
          in: query
          description: postcode values that need to be considered for filter
          required: false
          explode: true
          schema:
            type: string
        - name: postcode_mode
          in: query
          description: postcode mode values that need to be considered for filter type greater than,less than,equal
          required: false
          explode: true
          schema:
            type: string
            default: contains
            enum:
              - equals
              - contains
              - startsWith
              - endsWith
              - notContains
        - name: status
          in: query
          description: status values that need to be considered for filter
          required: false
          explode: true
          schema:
            type: integer
        - name: status_mode
          in: query
          description: status mode values that need to be considered for filter type greater than,less than,equal
          required: false
          explode: true
          schema:
            type: string
            default: equals
            enum:
              - equals
              - notEquals
              - lt
              - lte
              - gt
              - gte
      responses:
        '200':
          description: successful operation
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/definitions/customerData'
        '400':
          description: Invalid status value
/customer/suggestions:
    get:
      security:
          - bearerAuth: []
      tags:
        - customer
      summary: List customer
      description: Keyward search data on name,address,email,phone,city,state,postcode
      parameters:
        - name: search  
          in: query
          description: keyword search on name,address,email,phone,city,state,postcode
          required: false
          explode: true
          schema:
            type: string
      responses:
        '200':
          description: successful operation
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/definitions/customerData'
        '400':
          description: Invalid status value
/customer/{id}:
    get:
      security:
          - bearerAuth: []
      tags:
        - customer
      summary: List customer
      description: get 
      parameters:
        - name: id
          in: path
          description: get Customer by id
          required: true
          schema:
            type: string
      responses:
        '200':
          description: successful operation
          content:
            application/json:
              schema:
                type: object
                $ref: '#/definitions/customerData'
        '400':
          description: Invalid status value
definitions:
  customer:
      type: object
      properties:
        
        name:
            type: string
        address:
            type: string
        email:
            type: string
        phone:
            type: string
        createBy:
            type: string
        createAt:
            type: string
            format: date-time
        updateBy:
            type: string
        updateAt:
            type: string
            format: date-time
        city:
            type: string
        state:
            type: string
        postcode:
            type: string
        status:
            type: number
            format: double
              

  customerData:
      type: object
      properties:
      
        name:
            type: string
        address:
            type: string
        email:
            type: string
        phone:
            type: string
        createBy:
            type: string
        createAt:
            type: string
            format: date-time
        updateBy:
            type: string
        updateAt:
            type: string
            format: date-time
        city:
            type: string
        state:
            type: string
        postcode:
            type: string
        status:
            type: number
            format: double
              
    